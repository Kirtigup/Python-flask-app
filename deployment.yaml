apiVersion: v1
kind: Deployment
metadata:
  name : myapp-deployment
spec:
 type: NodePort
 selector:
    app: my-app
    type: front-end
 ports:
  - targetPort: 80
    port:80
    nodeport: 5000
 replicas: 2
